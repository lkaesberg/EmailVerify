node_modules
package-lock.json
config/config.json
.idea
config/bot.db
# SQLite's sidecar files. The -wal in particular holds pages not yet checkpointed
# into bot.db, i.e. real database content, so it must be ignored alongside it.
config/bot.db-*
src/bot.db
config/ServerStats.json
discord-verify.txt
config/ServerStatsHistory.log
site
.playwright-mcp
*.pyc